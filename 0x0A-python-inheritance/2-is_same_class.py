#!/usr/bin/python3
'''class module to check'''


def is_same_class(obj, a_class):
    '''function that returns True if the object is instance of class'''
    if (type(obj) == a_class):
        return (True)
    else:
        return (False)
